import org.junit.Assert;
import org.junit.Test;

public class User {
    String speak = "";


    @Test
    public void TestUserPetCountResponse() {
        //Given
        User user = new User(); // Create a user so they can respond
        // When
        // ask user how many pets do you have?
        //Then
        Assert.assertTrue();
    }

    @Test
    public void TestUserPetList() {
        //Given
        User user = new User(); // Create a user so they can list each of their pets
        //When
        //ask user what each kind of pet they have
        //Then
        // generate code that checks to see if user responce is true
    }
}
